xively-file-add-detector
========================

Posts data to xively indicating whether or not files have been added to a directory. Especially useful when combined with Yawcam's motion detection features.
